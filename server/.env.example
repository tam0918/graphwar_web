# Copy/rename this file to .env
# Windows PowerShell: Copy-Item .env.example .env

# Server port
PORT=8080

# AI hints provider (FPT Cloud - OpenAI compatible)
# Create a key in FPT Cloud and set it here
FPT_API_KEY=YOUR_FPT_CLOUD_API_KEY_HERE

# OpenAI-compatible base URL for FPT Cloud
# Tip: most OpenAI-compatible gateways serve under /v1
FPT_BASE_URL=https://mkp-api.fptcloud.com/v1

# Model name
FPT_MODEL=gpt-oss-120b

# Optional: request timeout (ms) for AI hint calls
FPT_TIMEOUT_MS=60000

# Optional: maximum LLM attempts when the suggested trajectory collides with terrain.
# The server will simulate each attempt; if it collides, it sends feedback and retries.
AI_HINT_MAX_ATTEMPTS=10

# --- MariaDB / MySQL (stats) ---
# If not set, server runs fine without DB; stats/leaderboard will be empty.
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_DATABASE=graphwar
DB_CONNECTION_LIMIT=5
